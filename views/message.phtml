<?php
if(isset($msg) && count($msg) > 0)
{
    foreach($msg as $msgData)
    {
        $msgType = $msgData['type'];
        $msgText = $msgData['text'];
        echo("<div class='alert alert-error'><button class='close' data-dismiss='alert'>&times;</button>" . $msgText . "</div>");
    }
}
