<?php
var_dump('su tu');
var_dump($msg);
if(isset($msg) && count($msg) > 0)
{
    var_dump('nesu tu');
    foreach($msg as $text)
    {
        echo("<div class='alert alert-error'><button class='close' data-dismiss='alert'>&times;</button>" . $text . "</div>");
    }
}
var_dump('byl sem tu');
